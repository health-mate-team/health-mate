#!/bin/sh
# Reject commit messages containing "with cursor" (any case).
set -e
MSG_FILE="$1"
if [ ! -f "$MSG_FILE" ]; then
  exit 0
fi
if grep -qiE 'with[[:space:]]+cursor' "$MSG_FILE"; then
  echo "commit-msg: 메시지에 'with cursor' 문구를 넣을 수 없습니다." >&2
  exit 1
fi
exit 0
